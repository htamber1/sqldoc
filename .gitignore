venv/
.env
__pycache__/
*.pyc
*.html
# Packaging build artifacts
*.egg-info/
build/
dist/
# May contain DB credentials; commit .sqldoc.example.yml instead.
.sqldoc.yml
# Generated schema snapshots for change detection (commit intentionally if you
# want cross-commit / CI diffing).
.sqldoc-snapshots/
# Cached AI descriptions (regenerated as needed).
.sqldoc-cache/
# PII-scan finding snapshots for drift detection.
.sqldoc-pii-snapshots/
htmlcov/
.coverage
coverage.xml
coverage.json

# security scan artifacts
bandit*.json
semgrep*.json
.secrets.baseline.tmp

# live-validation config (real secrets — never commit)
tests/live/sqldoc.live.yml
sqldoc.live.yml
